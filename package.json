{
  "name": "kravy-tracker",
  "version": "1.0.0",
  "description": "A full-stack RuneScape clan tracker.",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node src/server.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
